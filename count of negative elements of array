#include <stdio.h>
int main()
{
    int sum=0,arr[5];
    printf("Please enter 5 numbers:\n");
    for(int i=0; i < 5; i++){
        scanf("%d", &arr[i]);
    }
    for(int i=0; i < 5; i++){
        if(arr[i]<0){
            sum++;
        }
    }
    printf("%d\n", sum);
    return 0;
}
