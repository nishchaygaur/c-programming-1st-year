#include <stdio.h>

int main()
{
    char c;
    printf("enter value");
    scanf("%c",&c);
    if ((c >= 'a' && c<= 'z') || (c>='A' && c<='Z'))
    {
        printf("is alphabet");
    }
    else
    {
        printf("not alphabet");
    }

    return 0;
}
